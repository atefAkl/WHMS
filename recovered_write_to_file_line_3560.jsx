"import json\nimport re\n\nlog_path = r\"C:\\Users\\DELL\\.gemini\\antigravity\\brain\\5ffad68d-da27-4183-9b04-75222f1258f9\\.system_generated\\logs\\transcript.jsonl\"\n\nprint(\"Scanning log...\")\nviews = []\nwith open(log_path, \"r\", encoding=\"utf-8\") as f:\n    for line in f:\n        try:\n            data = json.loads(line)\n            # Check if it is a view_file tool call output for ContractSettings.jsx\n            if data.get(\"type\") == \"VIEW_FILE\" and \"ContractSettings.jsx\" in data.get(\"content\", \"\"):\n                content = data[\"content\"]\n                # Extract line range and content\n                lines = content.split(\"\\n\")\n                showing_match = re.search(r\"Showing lines (\\d+) to (\\d+)\", content)\n                if showing_match:\n                    start_line = int(showing_match.group(1))\n                    end_line = int(showing_match.group(2))\n                    print(f\"Found view for lines {start_line} to {end_line}\")\n                    \n                    # Extract the lines of code\n                    code_lines = {}\n                    for l in lines:\n                        m = re.match(r\"^(\\d+):\\s(.*)$\", l)\n                        if m:\n                            line_num = int(m.group(1))\n                            line_content = m.group(2)\n                            code_lines[line_num] = line_content\n                    views.append((start_line, end_line, code_lines))\n        except Exception as e:\n            pass\n\n# Merge views\nfull_code = {}\nfor start, end, code_lines in views:\n    full_code.update(code_lines)\n\nprint(f\"Total unique lines recovered: {len(full_code)}\")\nif len(full_code) > 0:\n    min_line = min(full_code.keys())\n    max_line = max(full_code.keys())\n    print(f\"Line range: {min_line} to {max_line}\")\n    \n    # Write to recovered file\n    out_path = r\"C:\\laragon\\www\\WHMS\\resources\\js\\Pages\\Settings\\ContractSettings_recovered.jsx\"\n    with open(out_path, \"w\", encoding=\"
<truncated 268 bytes>