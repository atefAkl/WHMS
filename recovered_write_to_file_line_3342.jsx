"import React, { useState, useRef, useEffect } from \"react\";\nimport AuthenticatedLayout from \"@/Layouts/AuthenticatedLayout\";\nimport { Head, router, useForm } from \"@inertiajs/react\";\nimport axios from \"axios\";\nimport { useLang } from \"@/Contexts/LanguageContext\";\nimport {\n    Home,\n    ChevronRight,\n    FileText,\n    LayoutList,\n    Clock,\n    Save,\n    X,\n    GripVertical,\n    Check,\n    Plus,\n    Edit,\n    Trash2,\n    Info,\n    Search,\n    Building2,\n    Variable,\n    Upload,\n    Eye,\n    EyeOff,\n    CheckCircle2,\n    ArrowRight,\n    UserCheck,\n    Calendar,\n    Printer,\n} from \"lucide-react\";\nimport Modal from \"@/Components/Modal\";\nimport TextInput from \"@/Components/TextInput\";\nimport InputLabel from \"@/Components/InputLabel\";\nimport InputError from \"@/Components/InputError\";\nimport PrimaryButton from \"@/Components/PrimaryButton\";\nimport SecondaryButton from \"@/Components/SecondaryButton\";\nimport DangerButton from \"@/Components/DangerButton\";\nimport Tooltip from \"@/Components/Tooltip\";\nimport PageHeader from \"@/Components/PageHeader\";\n\nexport default function ContractSettings({ terms: initialTerms, settings, blocks }) {\n    const { lang } = useLang();\n    const [activeTab, setActiveTab] = useState(\"company\"); // company, library, parts, periods, preview\n    const [terms, setTerms] = useState(initialTerms || []);\n    const [localBlocks, setLocalBlocks] = useState(blocks || []);\n    const [showTermModal, setShowTermModal] = useState(false);\n    const [editingTerm, setEditingTerm] = useState(null);\n    const [deleteTarget, setDeleteTarget] = useState(null);\n    const [saving, setSaving] = useState(false);\n    const [savingBlockId, setSavingBlockId] = useState(null);\n    const [blockSuccessMsg, setBlockSuccessMsg] = useState({});\n\n    // Synchronize props when they change\n    useEffect(() => {\n        setTerms(initialTerms || []);\n    }, [initialTerms]);\n\n    useEffect(() => {\n        setLocalBlocks(blocks || []);\n    }, 
<truncated 103924 bytes>