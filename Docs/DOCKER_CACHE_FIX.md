# Docker Session & CORS Fixes

هذا الملف يوثق جميع التعديلات التي قمنا بها لحل مشاكل الاتصال (CORS) ومشكلة الجلسات المنتهية (419 PAGE EXPIRED) عند الانتقال لبيئة Docker.

## 1. مشكلة CORS
تم تعديل `config/cors.php` كالتالي:
- السماح بجميع الروابط `allowed_methods => ['*']`
- السماح لجميع الـ Origins المطلوبة وبشكل خاص `http://whm.apl:8080`.
- إبقاء `supports_credentials => true` للسماح بتبادل الكوكيز بشكل سليم.

## 2. مشكلة 419 PAGE EXPIRED (انقطاع الجلسات)
عندما يتم الانتقال إلى بيئة Docker، قد لا يتعرف Laravel على الرابط كـ "آمن" أو قد تتعارض كوكيز النطاق. تم التعديل في `env.docker`:
- `APP_URL=http://whm.apl:8080` (مطابقة الدومين الذي نستخدمه فعلياً)
- `SESSION_DOMAIN=null` (ليتم اعتماد الكوكيز على نفس الرابط بدون التقييد بـ subdomains معينة قد تتسبب برفض المتصفح للكوكيز).
- `SESSION_SECURE_COOKIE=false` و `SESSION_SAME_SITE=lax` (لتخفيف قيود المتصفحات مثل Chrome في الروابط المحلية غير المشفرة).
- `SANCTUM_STATEFUL_DOMAINS` أضفنا لها بورتات التطبيق للتأكد من أمان الجلسات.

## 3. التعامل مع الكاش (مهم جداً)
دائماً عند نقل المشروع لجهاز آخر أو بعد عمل تعديلات في ملفات الـ Environment (`.env` أو `.env.docker`) يجب تفريغ الكاش تماماً داخل بيئة Docker باستخدام الأمر التالي:

```bash
docker exec wms-app php artisan optimize:clear
```
*(تم مسح الكاش مرات عديدة للتأكد من أن السيرفر يقرأ الإعدادات الجديدة وليس المخبأة).*

## 4. نصائح للمتصفح (Chrome)
المتصفح يحتفظ بجلسات (Cookies) للروابط القديمة. إذا ظهر لك خطأ 419 مجدداً:
- قم بفتح "نافذة تصفح خفي" (Incognito).
- إذا نجح الدخول، يجب عليك مسح ملفات الارتباط (Cookies) الخاصة بـ `whm.apl` من إعدادات Chrome بشكل كامل (لجميع الأوقات All time).

## 5. استعادة كلمة المرور
تم إنشاء سكريبت `reset_pass.php` لإعادة تعيين كلمة مرور الإدارة المركزية `admin@wms.com` إلى `admin123`.
يمكن تنفيذه باستخدام:
```bash
docker exec wms-app php artisan tinker reset_pass.php
```
